#!/bin/sh

# Before starting compton, we need to disable Xfce native compositor, if present.
[ -x /usr/bin/xfconf-query ] && xfconf-query -c xfwm4 -p /general/use_compositing -s false

exec compton "${@}"
