#!/bin/bash

pci_format () {
    echo ${1//[.:]/${2}}
}

pci2display () {
    echo :$(( 16#$(pci_format ${1}) ))
}

address=${1}
shift
display=$(pci2display ${address})
seat_name=__fake-seat-$(pci_format ${address} -)__
exec Xorg ${display} "${@}" -seat ${seat_name} -dpms -s 0 -nocursor -noreset